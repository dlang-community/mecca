/// Universal constants
module mecca.lib.consts;

// Licensed under the Boost license. Full copyright information in the AUTHORS file

enum KB = 1024;         /// Kilobyte (binary based) = 2^10
enum MB = 1024*KB;      /// Megabyte (binary based) = 2^20
enum GB = 1024*MB;      /// Gigabyte (binary based) = 2^30
enum TB = 1024*GB;      /// Terabyte (binary based) = 2^40
enum PB = 1024*TB;      /// Petabyte (binary based) = 2^50

enum BITS_IN_BYTE = 8;
